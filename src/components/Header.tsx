import { Component, ReactNode } from "react";

class Header extends Component {
  render(): ReactNode {
    return(
      <div>
        HEADER COMPONENT
      </div>
    )
  }
}

export default Header;
